## A baseline LLM smart contract auditor

This is a baseline LLM smart contract auditor that uses the OpenAI API to audit smart contracts.

## Usage

```
pnpm install
```

```
pnpm tsx ./src/index.ts
```

## Results

The results are saved in the `all-results.json` file.
